services:
  postgres:
    image: postgres:16
    container_name: jprime-connect-db
    environment:
      POSTGRES_DB: jprime_connect
      POSTGRES_USER: jprime
      POSTGRES_PASSWORD: jprime
    ports:
      - "5432:5432"
    tmpfs:
      - /var/lib/postgresql/data
